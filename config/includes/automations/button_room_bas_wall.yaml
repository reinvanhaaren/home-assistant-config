---
- alias: 'Button: Room Bas bed: on'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: sensor.button_room_bas_wall
      to: 'on'
  action:
    - service: light.turn_on
      data:
        entity_id: light.room_bas
- alias: 'Button: Room Bas bed: off'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: sensor.button_room_bas_wall
      to: 'off'
  action:
    - service: light.turn_off
      data:
        entity_id: light.room_bas
- alias: 'Button: Room Bas: brightness_down'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: sensor.button_room_bas_wall
      to: 'brightness_down'
  action:
    - service: light.turn_off
      data:
        entity_id: light.all
    - service: switch.turn_off
      data:
        entity_id: switch.living_room_corner
    - service: media_player.turn_off
      data:
        entity_id: media_player.chromecast
    - service: media_player.turn_off
      data:
        entity_id: media_player.tv
    - service: input_boolean.turn_off
      data:
        entity_id: input_boolean.guest_mode
